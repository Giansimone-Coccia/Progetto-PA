{
  "development": {
    "username": "${MYSQL_USER}",
    "password": "${MYSQL_PASSWORD}",
    "database": "${MYSQL_DATABASE}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  },
  "test": {
    "username": "${MYSQL_USER}",
    "password": "${MYSQL_PASSWORD}",
    "database": "${MYSQL_DATABASE}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  },
  "production": {
    "username": "${MYSQL_USER}",
    "password": "${MYSQL_PASSWORD}",
    "database": "${MYSQL_DATABASE}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  }
}
